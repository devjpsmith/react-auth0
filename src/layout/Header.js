import UserProfile from "../components/UserProfile";
import './Header.css'

export default function() {
    return (
        <header className="header">
            <UserProfile />
        </header>
    );
}